package com.taskBoard.Modules.TaskBoard.src.Mappings.BoardMappings;

import com.taskBoard.Models.Boards.Board;
import com.taskBoard.Modules.TaskBoard.src.Dto.Boards.BoardDto;
import org.mapstruct.Mapper;
import org.mapstruct.Mapping;

import java.util.List;

@Mapper
public interface BoardMapper {

//    @Mapping(target = "id", source = "UUID")
//    BoardDto modelToDto (Board board);
//    List<BoardDto> modelsToDtos (List<Board> boards);

//    @Mapping(target = "uuid", source = "id")
//    Board dtoToModel (BoardDto boardDto);
//    List<Board> dtosToModels (List<BoardDto> boardDtos);

}
